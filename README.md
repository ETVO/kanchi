# KanChi

## Running locally

Open `index.html` in a browser (or serve this folder with any static HTTP server).

Changes are persisted in `localStorage` in the browser and do not require any backend.
